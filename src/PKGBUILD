pkgname=('micsync')
pgkver=('0.0.1')
pkgrel=('1')
pkgdesc
arch=('any')
url
license=('GPL3')
depends=('rsync>=3.1.3' 'python')
makedepends=('python-setuptools')
source=('https://test.pypi.io/packages/source/m/micsync/micsync-0.0.1.tar.gz')
#md5sums

build() {
	python setup.py build
}

package() {
	python setup.py install --root="$pkgdir/" --optimize=1 --skip-build
}

